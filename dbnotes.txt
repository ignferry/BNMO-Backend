user
    id
    username
    password
    name
    ktp_image
    balance
    is_verified
    is_admin

request
    id
    requestor_id -> fk user.id
    amount
    status (0 => pending, 1 => declined, 2 => accepted)

transfer
    id
    sender_id -> fk user.id
    receiver_id -> fk user.id
    amount

